spring:
  application:
    name: baseline-service
  profiles:
    active: dev

server:
  port: 8081
  servlet:
    context-path: /

logging:
  level:
    root: INFO
    com.urbanpulse: DEBUG

urbanpulse:
  baseline:
    csv-path: "../../data/city_day.csv"
    default-city: "Mumbai"
